apiVersion: v1
kind: Service
metadata:
  name: {{ include "moniple-agent.fullname" . }}
  labels:
    {{- include "moniple-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "moniple-agent.selectorLabels" . | nindent 4 }}
